if (typeof cornerstone === 'undefined') {
    cornerstone = {};
}

if (typeof dicomParser === 'undefined') {
    dicomParser = {};
}

if (typeof cornerstoneTools === 'undefined') {
    cornerstoneTools = {
        referenceLines: {},
        orientation: {}
    };
}
